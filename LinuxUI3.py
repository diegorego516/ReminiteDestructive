import os
import sys
import argparse
import subprocess

def importar_modulos():
  comando = "LinuxUI3 " \
  "-Set UI-LinuxUI3 Modules-Enable - Set-MVP-Model-SetView-Controller"
def coletar_dados_de_interface_gráfica():
  coletar_dados_de_interface_gráfica = True
def get_user_graphical_interface():
 target = target.user.graphical.interface.getusertargetmachine("Endereço IP: ")
def escanear_por_portas_abertas():
  escanear_por_portas_abertas = True
def portas_abertas_de_alvos():
  input(f"Portas abertas: {escanear_por_portas_abertas} portas_abertas_do_alvo: ")
  return portas_abertas_de_alvos

import scapy
  # Monta a estaca de interface gráfica do usuário: CLI + UI na estaca
def LinuxUI3(CLI_UI):
    pacote = CLI_UI(CLI_UI=get_user_graphical_interface)(CLI_UI, Tool="SystemHack")
    resposta = CLI_UI(pacote, timeout=1, verbose=0)


    if resposta in resposta.haslayer(pacote):
            resposta.getlayer(pacote).flags == 0x12
            print(f"Pacote foi compilado com sucesso.")
    else:
            print(f"Pacote não foi compilado com sucesso.")

    import subprocess
    def criar_interface_gráfica_de_usuário(LinuxUI3):
        modulo = "Command -Set UI-LinuxUI3 Modules-Enable " \
        " - Set-UI3-Module-Enable-System-Analyst-Settings"
    def comando():
        try:
          subprocess.run(comando, shell=True)
          print("Ativando interface gráfica de usuário " \
          "para analise de sistema por portas abertas...")
        except Exception as e:
            KeyboardInterrupt

def coletar_dados():

    importar_modulos()
    coletar_dados()

def LinuxUI3(CLI_UI):

    LinuxUI3(CLI_UI, timeout=1, verbose=0)
